{
  "name": "@ares/cli",
  "version": "0.16.0",
  "type": "module",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Ares v2 CLI: the `ares` binary. Ink-based TUI, slash commands, session DAG navigation.",
  "main": "dist/entry.js",
  "bin": {
    "ares": "dist/entry.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "test:tui": "node --test tests/tui/*.test.mjs"
  },
  "dependencies": {
    "@ares/agent": "workspace:*",
    "@ares/channels": "workspace:*",
    "@ares/connectors": "workspace:*",
    "@ares/core": "workspace:*",
    "@ares/effects": "workspace:*",
    "@ares/garrison": "workspace:*",
    "@ares/mind": "workspace:*",
    "@ares/operator": "workspace:*",
    "@ares/protocol": "workspace:*",
    "@ares/tools": "workspace:*",
    "ink": "^7.0.4",
    "react": "^19.2.6",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "ink-testing-library": "^4.0.0"
  }
}
