{
  "name": "@crix/connectors",
  "version": "0.5.0-alpha.1",
  "type": "module",
  "private": true,
  "license": "MIT",
  "description": "Crix v5 connectors: the reach into the world. A vendor-neutral BrowserConnector (DOM-first + screenshot), browser actions as audited Effects, and the screenshot filmstrip. Playwright is an opt-in engine adapter; a deterministic mock backs tests.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@crix/effects": "workspace:*"
  },
  "optionalDependencies": {
    "playwright": "^1.49.0"
  }
}
